<Page x:Class="VaultDataAPISampleApp.Features.FileUpload.Views.FileUploadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      d:DesignHeight="650"
      d:DesignWidth="1000"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.ColumnSpan="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock FontSize="22"
                           FontWeight="SemiBold"
                           Text="Upload a file to Vault"/>
                <TextBlock Grid.Column="1"
                           Margin="0,0,12,0"
                           VerticalAlignment="Center"
                           Text="Loading Vault root..."
                           Visibility="{Binding IsLoadingRoot, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Grid.Column="2"
                        MinWidth="80"
                        Command="{Binding RefreshTreeCommand}"
                        Content="Refresh Tree"/>
            </Grid>

            <Grid Grid.Row="1" Grid.ColumnSpan="3" Margin="0,20,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="Local file"/>
                <TextBox Grid.Column="1"
                         Margin="0,0,8,0"
                         IsReadOnly="True"
                         Text="{Binding LocalFilePath}"/>
                <Button Grid.Column="2"
                        MinWidth="90"
                        Command="{Binding BrowseCommand}"
                        Content="Browse..."/>
            </Grid>

            <GroupBox Grid.Row="2" Grid.Column="0" Header="Vault contents">
                <TreeView Height="350"
                          Padding="6"
                          ItemsSource="{Binding VaultContents}">
                    <TreeView.Resources>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Setter Property="IsExpanded"
                                    Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            <Setter Property="IsSelected"
                                    Value="{Binding IsSelected, Mode=TwoWay}"/>
                        </Style>
                    </TreeView.Resources>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock VerticalAlignment="Center"
                                           FontWeight="SemiBold"
                                           Text="{Binding DisplayName}"/>
                                <TextBlock Margin="8,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           Text="{Binding Detail}"/>
                                <ProgressBar Width="45"
                                             Height="4"
                                             Margin="8,0,0,0"
                                             VerticalAlignment="Center"
                                             IsIndeterminate="True"
                                             Visibility="{Binding IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <StackPanel Grid.Row="2" Grid.Column="2">
                <GroupBox Header="Add a new file">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock FontWeight="SemiBold"
                                   Text="{Binding SelectedNode.Folder.FullName}"/>
                        <TextBlock Grid.Row="1"
                                   Margin="0,10,0,14"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Text="Select a destination folder in the tree."
                                   TextWrapping="Wrap"/>
                        <Button Grid.Row="2"
                                HorizontalAlignment="Right"
                                MinWidth="130"
                                Command="{Binding AddFileCommand}"
                                Content="Add New File"/>
                    </Grid>
                </GroupBox>

                <GroupBox Margin="0,16,0,0" Header="Check in an existing file">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock FontWeight="SemiBold"
                                   Text="{Binding SelectedNode.File.Name}"/>
                        <CheckBox Grid.Row="1"
                                  Margin="0,12,0,0"
                                  IsChecked="{Binding CheckoutFirst}"
                                  Content="Check out before uploading"/>
                        <CheckBox Grid.Row="2"
                                  Margin="0,8,0,0"
                                  IsChecked="{Binding KeepCheckedOut}"
                                  Content="Keep checked out after check-in"/>
                        <TextBlock Grid.Row="3"
                                   Margin="0,10,0,14"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Text="Select the Vault file to update. Clear the first option if it is already checked out by the current user."
                                   TextWrapping="Wrap"/>
                        <Button Grid.Row="4"
                                HorizontalAlignment="Right"
                                MinWidth="130"
                                Command="{Binding CheckinFileCommand}"
                                Content="Check In File"/>
                    </Grid>
                </GroupBox>
            </StackPanel>

            <Border Grid.Row="2"
                    Grid.Column="1"
                    Width="1"
                    HorizontalAlignment="Center"
                    Background="{DynamicResource SeparatorBorderBrush}"/>
            </Grid>
        </ScrollViewer>

        <StatusBar Grid.Row="1">
            <StatusBarItem DockPanel.Dock="Right">
                <Button MinWidth="80"
                        Command="{Binding CancelCommand}"
                        Content="Cancel"/>
            </StatusBarItem>
            <StatusBarItem DockPanel.Dock="Right">
                <ProgressBar Width="200"
                             Height="6"
                             IsIndeterminate="{Binding IsProgressIndeterminate, Mode=OneWay}"
                             Maximum="100"
                             Value="{Binding ProgressPercentage, Mode=OneWay}"
                             Visibility="{Binding IsBusy, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
            <Separator DockPanel.Dock="Right"/>
            <StatusBarItem>
                <TextBlock Text="{Binding Status}"
                           TextTrimming="CharacterEllipsis"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Page>
